datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
 
generator client {
  provider = "prisma-client-js"
}
 
model Role {
  id            Int      @id @default(autoincrement())  // ID autoincremental
  name          String   @unique
  description   String?  

  users         User[]   // Relación de uno a muchos con el modelo User
}

model User {
  id            String          @id @default(cuid())
  name          String?
  email         String          @unique
  emailVerified DateTime?
  image         String?
  phone         String?         // Número de teléfono opcional
  roleId        Int             @default(1)   // Llave foránea a la tabla Role
  
  accounts      Account[]
  documents     Document[]
  mentor        Mentor?
  student       Student?
  authority     Authority?
  
  role          Role?           @relation(fields: [roleId], references: [id])

  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt
}

model Mentor {
  id            Int             @id @default(autoincrement())
  userId        String          @unique
  
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt

  mentorGroups  MentorGroup[]   // Relación de muchos a muchos con grupos

  user          User            @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Student {
  id                  Int       @id @default(autoincrement())
  userId              String    @unique // Llave foránea que apunta a User
  groupId             Int? 
  subjectId           Int?      // Relación con la tabla `Subject`

  registrationNumber  String?   // Número de matrícula del estudiante
  semester            String?

  user                User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  group               Group?    @relation(fields: [groupId], references: [id], onDelete: SetNull)
  subject             Subject?  @relation(fields: [subjectId], references: [id])
}

model Group {
  id            Int             @id @default(autoincrement())
  name          String          // Nombre o título del grupo
  titleProject  String?         // Título del trabajo de investigación
  subjectId     Int             @default(1)

  students      Student[]       // Relación de uno a muchos con estudiantes
  mentors       MentorGroup[]   // Relación de muchos a muchos con mentores
  subject       Subject         @relation(fields: [subjectId], references: [id])

  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt

  @@unique([name, subjectId])
}

model MentorGroup {
  mentorId      Int
  groupId       Int

  mentor        Mentor  @relation(fields: [mentorId], references: [id], onDelete: Cascade)
  group         Group   @relation(fields: [groupId], references: [id], onDelete: Cascade)

  @@id([mentorId, groupId])  // Llave primaria compuesta
}

model Subject {
  id                  Int       @id @default(autoincrement())
  name                String

  students            Student[]
  groups              Group[]
}

model Authority {
  id            Int             @id @default(autoincrement())
  userId        String          @unique  // user_id INT NOT NULL

  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt

  user          User            @relation(fields: [userId], references: [id])
}

model Account {
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?

  createdAt         DateTime     @default(now())
  updatedAt         DateTime     @updatedAt

  user              User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@id([provider, providerAccountId])
  @@unique([userId, provider])
}

model Document {
  id              Int       @id @default(autoincrement())
  name            String    // Nombre del documento
  typeId          Int       // Llave foránea que apunta a DocumentType
  userId          String    // Llave foránea a User
  uploadDate      DateTime  @default(now()) // Fecha de subida
  size            Float     // Tamaño del archivo en bytes
  content         Bytes     // Contenido binario del archivo

  user            User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  documentType    DocumentType @relation(fields: [typeId], references: [id])
}

model DocumentType {
  id              Int       @id @default(autoincrement())
  name            String    

  documents       Document[]
}